galaxy_info:
  author: Arnaud Blancher
  description:  dist-upgrade debian/ubuntu and waiting for ssh is up again
  license: GPLv3
  min_ansible_version: 2.0

  platforms:
  - name: Ubuntu
    versions:
    - precise
    - trusty
    - xenial
  - name: Debian
    versions:
    - jessie

  galaxy_tags:
    - system
    - apt
    - apt dist-upgrade
    - reboot
    - ssh

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.or ssh is uop agin

